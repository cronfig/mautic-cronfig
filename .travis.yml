language: php

php:
  - 7.2
  - 7.3

before_install:
  # turn off XDebug
  - phpenv config-rm xdebug.ini || return

  # install dependencies in parallel
  - travis_retry composer global require hirak/prestissimo

  # PHPSTAN somehow stopped working... No time to deal with it now...
  # - composer require --dev phpstan/phpstan-shim

before_script:
  - cd ..
  - git clone https://github.com/mautic/mautic.git mautic
  - cd mautic
  - composer install
  - mv $TRAVIS_BUILD_DIR plugins
  - mv plugins/mautic-cronfig plugins/CronfigBundle
  - ls plugins/CronfigBundle

script:
  - bin/phpunit --bootstrap vendor/autoload.php --configuration app/phpunit.xml.dist --filter CronfigBundle
  - bin/php-cs-fixer fix plugins/CronfigBundle -v --dry-run --diff --rules=@Symfony
  # - vendor/bin/phpstan.phar analyse plugins/CronfigBundle
  # - vendor/bin/phpstan.phar analyse plugins/CronfigBundle/Controller -l 1
  # - vendor/bin/phpstan.phar analyse plugins/CronfigBundle/Model -l 7
